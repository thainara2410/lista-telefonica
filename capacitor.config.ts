import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lista_telefonica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
